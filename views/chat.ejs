<%- include('partials/header', { title: title }) %>

<style>
  #chat-box {
    max-width: 640px;
    border: 1px solid #ddd;
    border-radius: 8px;
    height: 460px;
    overflow-y: auto;
    padding: 16px;
    background: #ededed;
  }
  .msg-row { display: flex; margin-bottom: 12px; }
  .msg-row.user { justify-content: flex-end; }
  .msg-row.ai { justify-content: flex-start; }
  .bubble {
    max-width: 70%;
    padding: 8px 12px;
    border-radius: 8px;
    line-height: 1.5;
    white-space: pre-wrap;
    word-break: break-word;
  }
  .msg-row.user .bubble { background: #95ec69; }
  .msg-row.ai .bubble { background: #fff; }
  .bubble.pending { color: #999; font-style: italic; }
  #chat-input-area { max-width: 640px; margin-top: 12px; display: flex; gap: 8px; }
  #chat-input { flex: 1; resize: vertical; padding: 8px; font-size: 14px; }
  #quota-tip { max-width: 640px; margin: 8px 0; color: #888; font-size: 14px; }
  #quota-empty { max-width: 640px; margin: 8px 0; color: #d00; font-size: 14px; }
</style>

<h1>💬 对话闲人</h1>

<% if (freeChatCount > 0) { %>
  <div id="quota-tip">剩余免费次数：<strong id="quota-num"><%= freeChatCount %></strong> 次</div>
<% } else if (hasOwnKey) { %>
  <div id="quota-tip">免费次数已用完，正在使用你自己的 API Key（不限次）</div>
<% } else { %>
  <div id="quota-empty">
    免费次数已用完。请前往 <a href="/settings">设置页</a> 填写你自己的 DeepSeek API Key 后继续使用。
  </div>
<% } %>

<div id="chat-box"></div>

<div id="chat-input-area">
  <textarea id="chat-input" rows="2" placeholder="和闲人聊点什么…（Enter 发送，Shift+Enter 换行）"></textarea>
  <button id="send-btn">发送</button>
</div>

<script src="/js/utils.js"></script>
<script>
  (function () {
    var box = document.getElementById('chat-box');
    var input = document.getElementById('chat-input');
    var sendBtn = document.getElementById('send-btn');
    var quotaNum = document.getElementById('quota-num');
    var sending = false;

    function addMessage(role, text, pending) {
      var row = document.createElement('div');
      row.className = 'msg-row ' + role;
      var bubble = document.createElement('div');
      bubble.className = 'bubble' + (pending ? ' pending' : '');
      // escapeHtml 来自 /js/utils.js，防止把用户/AI 文本当 HTML 解析
      bubble.innerHTML = escapeHtml(text);
      row.appendChild(bubble);
      box.appendChild(row);
      box.scrollTop = box.scrollHeight;
      return bubble;
    }

    function send() {
      if (sending) return;
      var text = input.value.trim();
      if (!text) return;

      addMessage('user', text);
      input.value = '';
      sending = true;
      sendBtn.disabled = true;
      var pendingBubble = addMessage('ai', '闲人正在输入…', true);

      fetch('/api/chat', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ message: text })
      })
        .then(function (res) {
          if (res.status === 401) { window.location.href = '/login'; return null; }
          return res.json().then(function (data) { return { ok: res.ok, data: data }; });
        })
        .then(function (result) {
          if (!result) return;
          if (!result.ok) {
            pendingBubble.className = 'bubble';
            pendingBubble.textContent = '⚠️ ' + (result.data.error || '出错了');
            return;
          }
          pendingBubble.className = 'bubble';
          pendingBubble.innerHTML = escapeHtml(result.data.reply);
          // remaining 为 null 表示在用用户自己的 Key，不更新免费次数
          if (result.data.remaining !== null && quotaNum) {
            quotaNum.textContent = result.data.remaining;
          }
        })
        .catch(function (err) {
          pendingBubble.className = 'bubble';
          pendingBubble.textContent = '⚠️ 网络错误，请稍后再试';
          console.error(err);
        })
        .finally(function () {
          sending = false;
          sendBtn.disabled = false;
          box.scrollTop = box.scrollHeight;
        });
    }

    sendBtn.addEventListener('click', send);
    // Enter 发送，Shift+Enter 换行
    input.addEventListener('keydown', function (e) {
      if (e.key === 'Enter' && !e.shiftKey) {
        e.preventDefault();
        send();
      }
    });
  })();
</script>

<%- include('partials/footer') %>
